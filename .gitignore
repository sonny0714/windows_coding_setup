
# md_files (per-user notes and worklog; not shared)
md_files/

# Worklog commands (per-user, machine-specific)

# Per-user workspace (machine-specific: access.yaml secrets, user.md, run/)
md_files/users/

