
# md_files (per-user notes and worklog; not shared)
md_files/

# Worklog commands (per-user, machine-specific)

# Worklog (per-user, machine-specific)
md_files/users/users.yaml

# Claude Code workspace (generated per server by alias setup.sh)
.claude/
